<%= simple_form_for(@rank) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :from %>
    <%= f.input :to %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
